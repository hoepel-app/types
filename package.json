{
  "name": "@hoepel.app/types",
  "version": "1.3.0-beta.3",
  "description": "Interfaces and models for hoepel.app",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@types/lodash": "^4.14.112",
    "lodash": "^4.17.11",
    "virginity-ts": "^0.1.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.7",
    "chai": "^4.2.0",
    "husky": "^2.4.0",
    "mocha": "^6.1.4",
    "nyc": "^14.1.1",
    "ts-node": "^8.2.0",
    "tslint": "^5.12.0",
    "tslint-immutable": "^6.0.0",
    "typescript": "^3.5.1"
  },
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run lint && npm run build",
    "lint": "tslint --project tsconfig.json --config tslint.json",
    "lint-fix": "tslint --project tsconfig.json --config tslint.json --fix",
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "testWithCoverage": "nyc -r lcov -e .ts -x \"*.test.ts\" mocha -r ts-node/register tests/**/*.test.ts && nyc report"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thomastoye/types.hoepel.app.git"
  },
  "author": "Thomas Toye",
  "bugs": {
    "url": "https://github.com/thomastoye/types.hoepel.app/issues"
  },
  "homepage": "https://github.com/thomastoye/types.hoepel.app#readme",
  "husky": {
    "hooks": {
      "pre-commit": "npm run build && npm run lint && npm test"
    }
  }
}
